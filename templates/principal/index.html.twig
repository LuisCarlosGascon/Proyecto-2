{% extends 'base.html.twig' %}

{% block body %}
<main>    
    <section class="carousel-tamano">
        <div id="header-carousel" class="carousel slide carousel-fade h-50" data-bs-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <img class="w-100" src="{{asset('img/' ~ evento1.imagen)}}" alt="Image" height="400px">
                    <div class="carousel-caption d-flex flex-column align-items-center justify-content-center">
                        <div class="p-3" style="max-width: 900px;">
                            <h5 class="text-white text-uppercase mb-3 animated slideInDown">Xokas y Cía</h5>
                            <h1 class="display-1 text-white mb-md-4 animated zoomIn">{{evento1.nombre}}</h1>
                            {# <a href="quote.html" class="btn btn-primary py-md-3 px-md-5 me-3 animated slideInLeft">Free Quote</a>
                            <a href="" class="btn btn-outline-light py-md-3 px-md-5 animated slideInRight">Contact Us</a> #}
                        </div>
                    </div>
                </div>
            {% for evento in eventos %}
                <div class="carousel-item">
                    <img class="w-100" src="{{asset('img/' ~ evento.imagen)}}" alt="Image" height="400px">
                    <div class="carousel-caption d-flex flex-column align-items-center justify-content-center">
                        <div class="p-3" style="max-width: 900px;">
                            <h5 class="text-white text-uppercase mb-3 animated slideInDown">Xokas y Cía</h5>
                            <h1 class="display-1 text-white mb-md-4 animated zoomIn">{{evento.nombre}}</h1>
                            {# <a href="quote.html" class="btn btn-primary py-md-3 px-md-5 me-3 animated slideInLeft">Free Quote</a>
                            <a href="" class="btn btn-outline-light py-md-3 px-md-5 animated slideInRight">Contact Us</a> #}
                        </div>
                    </div>
                </div>
            {% endfor %}
            </div>
        </div>
    </section>

    {# <section>
        <div class="c-juegoRandom">
            <img class="w-100" src="{{asset('img/' ~ juego.imagen)}}">
            <button class="btn btn-outline-light">¡Juega ya!</button>
        </div>
    </section> #}

</main>

{% endblock %}
