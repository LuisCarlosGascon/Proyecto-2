{% extends 'base.html.twig' %}

{% block body %}
    {# <p>Hola</p>
    <h1>Que tal</h1> #}
   
    <div id="edificio">
        <div id="distribuciones">
            Disposición de la sala: 
            <select id="selectDis">
                <option selected disabled="true" id="default">Seleccione una fecha</option>
            </select>
            <button id="base" class="btn btn-primary">Base</button>
        </div>
        <div id="sala">  
        
        </div>
        <div id="trastero">
        
        </div>
    </div>
{% endblock %}

